apiVersion: keycloak.pewty.fr/v1
kind: Client
metadata:
  labels:
    app.kubernetes.io/name: keycloak-client-operator
    app.kubernetes.io/managed-by: kustomize
  name: client-sample
  namespace: golden-path-spring-app-uat
spec:
  realm: "test"
  # Reference to Kubernetes Secret containing client credentials
  secretRef:
    name: "golden-path-spring-app-uat-back-springconfig"
    # Optional: specify custom keys (defaults shown below)
    clientIdKey: "client.id"
    clientSecretKey: "client.secret"
  client:
    name: "My Client"
    enabled: true
    protocol: "openid-connect"
    publicClient: false
    directAccessGrantsEnabled: true
    redirectUris:
      - "https://myapp.example.com/*"
    webOrigins:
      - "*"

